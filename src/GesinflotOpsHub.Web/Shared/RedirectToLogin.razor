@inject NavigationManager Nav

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            Nav.NavigateTo($"/Identity/Account/Login?returnUrl={Uri.EscapeDataString(Nav.Uri)}", forceLoad: true);
        }
    }
}
